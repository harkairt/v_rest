name: v_rest
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: ">=3.0.6 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  clock: ^1.1.1
  dio: ^5.3.3
  flutter:
    sdk: flutter
  fpdart: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
